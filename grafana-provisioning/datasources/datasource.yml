apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influx:8086
    isDefault: true
    database: ripper
    jsonData:
      defaultBucket: ripper
      organization: faucet
      version: Flux
    secureJsonData:
      token: admin1000
